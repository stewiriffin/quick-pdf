# Design reference

Canonical UI source from the Figma Make export (`Exciting App UI Design.zip`).

| File | Role |
|------|------|
| `canonical/App.tsx` | Interactive app UI reference |
| `canonical/Screenshots.tsx` | Play Store screenshot layouts |
| `canonical/theme.css` | Color tokens |
| `canonical/fonts.css` | Plus Jakarta Sans |

Do not keep full zip extracts or `node_modules` here — re-extract the zip only when regenerating assets.
